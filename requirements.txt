numpy>=1.23
scipy>=1.10
scikit-learn>=1.6
giotto-tda>=0.6
matplotlib>=3.7

# Experiment runner
aeon>=1.0
tsfresh>=0.20
tqdm>=4.66

# Cross-persistence backend (install from source):
#   git clone https://github.com/IlyaTrofimov/MTopDiv external/MTopDiv
#   pip install -e external/MTopDiv
# MTopDiv requires CUDA + ripserplusplus (see external/MTopDiv/README.md).
#
# Install note: giotto-tda pins sklearn 1.3.2 as a dependency. After
# pip install -r requirements.txt, if aeon breaks, run:
#   pip install "scikit-learn>=1.6" --no-deps
