numpy>=1.23
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Cross-persistence backend (install from source):
#   git clone https://github.com/IlyaTrofimov/MTopDiv external/MTopDiv
#   pip install -e external/MTopDiv
# MTopDiv additionally requires torch and ripserplusplus (see external/MTopDiv/README.md).

# Optional CPU fallback when CUDA/MTopDiv is unavailable at runtime:
# ripser>=0.6
